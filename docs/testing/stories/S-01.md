# S-01 — Khung ứng dụng chạy được trên máy cá nhân

> **Dự án**: Charging-Station-Management-System-CSMS-  
> **Jira**: `S-01`, `T-01`  
> **Test Date**: 24/09/2026 (Re-tested sau khi bật Docker daemon)  
> **Người thực hiện**: TESTER / QA  

---

## 1. Requirement & Acceptance Criteria

### S-01
Khung ứng dụng chạy được trên máy cá nhân của thành viên mới thông qua hướng dẫn chuẩn trong README, bảo đảm an toàn cấu hình và kết nối cơ sở dữ liệu.

#### Acceptance Criteria
- **S01-AC-01**: Given thành viên mới lấy source code, When chạy command khởi động ghi trong README (`docker compose up --build app`), Then application chạy được, database chạy được, trang chủ trả về HTTP 200.

#### Non-functional Requirements
- **S01-NFR-01**: Secrets (`JWT_SECRET`, `DATABASE_URL`) phải lấy từ environment variables, không hard-code trong mã nguồn, config hay .env.example.
- **S01-NFR-02**: Log không được in database connection string, credentials hay password.

---

## 2. T-01 — Dựng khung dự án và kết nối cơ sở dữ liệu

- **T01-01**: `docker compose up` -> application + database hoạt động.
- **T01-02**: Database connection -> application kết nối PostgreSQL thành công.
- **T01-03**: Migration forward -> migration chạy sạch.
- **T01-04**: Migration rollback -> rollback thành công trên database test (`db_test`).
- **T01-05**: Naming convention schema: `snake_case`, primary key `id BIGSERIAL PRIMARY KEY`, timestamps `created_at`, `updated_at`.
- **T01-NFR-01**: Database connection string lấy từ environment (`DATABASE_URL`).

---

## 3. Test Execution & Evidence

### TC-S01-01
- **Test ID**: `TC-S01-01`
- **Jira**: `S-01`
- **Requirement**: `S01-AC-01` (Khởi động ứng dụng theo README, App/DB chạy, Trang chủ HTTP 200)
- **Preconditions**: Docker Desktop đang chạy trên máy host.
- **Steps**:
  1. Đọc hướng dẫn khởi động trong `README.md` mục 2 (Cách A: `docker compose up --build app`).
  2. Thực thi lệnh chính thức và kiểm tra trạng thái container: `docker compose ps`.
  3. Gửi request kiểm tra health: `curl.exe http://localhost:3000/api/health`.
  4. Gửi request kiểm tra trang chủ: `curl.exe -i http://localhost:3000/`.
- **Expected**: Container `app` và `db` đều chạy (`Up`), cơ sở dữ liệu kết nối thành công, trang chủ trả về HTTP 200.
- **Actual**:
  - `charging-station-management-system-csms--app-1`: Trạng thái `Up` (port 3000).
  - `charging-station-management-system-csms--db-1`: Trạng thái `Up (healthy)` (port 5432).
  - Endpoint `/api/health`: Trả về `{"ok":true,"service":"csms-backend","database":"postgresql"}`.
  - Trang chủ `http://localhost:3000/`: Trả về mã **HTTP 200 OK** với toàn bộ nội dung HTML đăng nhập.
- **Status**: **PASS**
- **Evidence**:
  ```text
  $ docker compose ps
  NAME                                             IMAGE                                          SERVICE   STATUS
  charging-station-management-system-csms--app-1   charging-station-management-system-csms--app   app       Up (healthy) 0.0.0.0:3000->3000/tcp
  charging-station-management-system-csms--db-1    postgres:16-alpine                             db        Up (healthy) 0.0.0.0:5432->5432/tcp

  $ curl.exe -i http://localhost:3000/
  HTTP/1.1 200 OK
  X-Powered-By: Express
  Content-Type: text/html; charset=utf-8
  Content-Length: 7121
  <!DOCTYPE html>
  <html lang="vi">...
  ```

### TC-S01-02
- **Test ID**: `TC-S01-02`
- **Jira**: `S-01`
- **Requirement**: `S01-NFR-01` (Secret nạp từ environment variables, không hard-code)
- **Preconditions**: Tệp `src/config/env.js`, `.env.example`, `backend/tests/unit/no-backdoor.test.js`
- **Steps**:
  1. Đọc mã nguồn `backend/src/config/env.js`.
  2. Kiểm tra tệp mẫu `.env.example`.
  3. Chạy test xác minh: `node --test tests/unit/no-backdoor.test.js`.
- **Expected**: `JWT_SECRET`, `DATABASE_URL` đọc từ `process.env`. Không có bí mật mặc định trong code hay migration.
- **Actual**: `env.js` nạp qua `process.env`. Bộ test `no-backdoor.test.js` chạy thành công 3/3 test cases.
- **Status**: **PASS**
- **Evidence**:
  ```text
  ▶ S-01: không backdoor, không bí mật mặc định
    ✔ package.json không còn pg-mem và bcryptjs (0.4376ms)
    ✔ mã nguồn và migration không còn seedAdminPassword, admin123, pg-mem, bcrypt, development-secret (4.7588ms)
    ✔ migration không seed user (0.5336ms)
  ✔ S-01: không backdoor, không bí mật mặc định (6.4485ms)
  ℹ tests 3, pass 3, fail 0
  ```

### TC-S01-03
- **Test ID**: `TC-S01-03`
- **Jira**: `S-01`
- **Requirement**: `S01-NFR-02` (Log không được in connection string, credentials hay password)
- **Preconditions**: Container logs `app` và `db`, mã nguồn `backend/src/config/env.js` và `backend/src/db/pool.js`
- **Steps**:
  1. Kiểm tra log thực tế của container `app`: `docker compose logs app --tail 50`.
  2. Kiểm tra log thực tế của container `db`: `docker compose logs db --tail 30`.
  3. Kiểm tra xử lý log trong `src/config/env.js` dòng 26-28 và `src/db/pool.js` dòng 10.
- **Expected**: Log không chứa database connection string, password hay secret.
- **Actual**: Container logs chỉ hiển thị trạng thái startup, applied migrations và `CSMS backend listening on http://localhost:3000`. Khi có lỗi, chỉ in tên biến (`names.join(', ')`) hoặc `error.message`. Hoàn toàn không lộ chuỗi URI kết nối hay mật khẩu.
- **Status**: **PASS**
- **Evidence**:
  ```text
  $ docker compose logs app --tail 10
  app-1  | CSMS backend listening on http://localhost:3000
  ```

### TC-T01-01
- **Test ID**: `TC-T01-01`
- **Jira**: `T-01`
- **Requirement**: `T01-01 / T01-02` (`docker compose up` & DB connection)
- **Preconditions**: Docker Compose v5.5.1
- **Steps**:
  1. Chạy `docker compose ps`.
  2. Gửi request `http://localhost:3000/api/health`.
- **Expected**: Cả service `app` và `db` hoạt động, app kết nối PostgreSQL thành công.
- **Actual**: Cả hai container đều `Up`. Endpoint `/api/health` xác nhận kết nối cơ sở dữ liệu `postgresql` thành công: `{"ok":true,"service":"csms-backend","database":"postgresql"}`.
- **Status**: **PASS**
- **Evidence**:
  ```json
  {"ok":true,"service":"csms-backend","database":"postgresql","time":"2026-09-24T12:03:40.483Z"}
  ```

### TC-T01-02
- **Test ID**: `TC-T01-02`
- **Jira**: `T-01`
- **Requirement**: `T01-03` (Migration forward)
- **Preconditions**: Cơ sở dữ liệu PostgreSQL đang chạy
- **Steps**:
  1. Kiểm tra log khởi động container `app`.
  2. Chạy migration forward trên database test `db_test`: `docker compose exec -e DATABASE_URL=postgresql://csms:csms_test_only@db_test:5432/csms_test app node src/db/migrate.js`.
- **Expected**: Toàn bộ migration chạy sạch và không phát sinh lỗi.
- **Actual**: Áp dụng thành công toàn bộ migration:
  - `Applied migration 001_baseline.sql`
  - `Applied migration 002_login_throttle_drop_user_lockout.sql`
  - `Applied migration 003_stations_owner.sql`
- **Status**: **PASS**
- **Evidence**: Terminal output exit code 0.

### TC-T01-03
- **Test ID**: `TC-T01-03`
- **Jira**: `T-01`
- **Requirement**: `T01-04` (Migration rollback trên database test)
- **Preconditions**: Service `db_test` cổng 5433 đang chạy
- **Steps**:
  1. Khởi chạy `db_test`: `docker compose up -d db_test`.
  2. Thực thi lệnh rollback: `docker compose exec -e DATABASE_URL=postgresql://csms:csms_test_only@db_test:5432/csms_test app node src/db/migrate.js down`.
  3. Thực thi tiếp lệnh rollback: `docker compose exec -e DATABASE_URL=postgresql://csms:csms_test_only@db_test:5432/csms_test app node src/db/migrate.js down`.
  4. Thực thi migrate forward lại: `docker compose exec -e DATABASE_URL=postgresql://csms:csms_test_only@db_test:5432/csms_test app node src/db/migrate.js`.
- **Expected**: Rollback migration thành công theo thứ tự lùi qua các file `.down.sql`, và re-apply thành công.
- **Actual**:
  - Rollback lần 1: `Rolled back migration 003_stations_owner.sql` (exit code 0).
  - Rollback lần 2: `Rolled back migration 002_login_throttle_drop_user_lockout.sql` (exit code 0).
  - Re-apply forward: `Applied migration 002...`, `Applied migration 003...` (exit code 0).
- **Status**: **PASS**
- **Evidence**:
  ```text
  $ docker compose exec ... node src/db/migrate.js down
  Rolled back migration 003_stations_owner.sql

  $ docker compose exec ... node src/db/migrate.js down
  Rolled back migration 002_login_throttle_drop_user_lockout.sql
  ```

### TC-T01-04
- **Test ID**: `TC-T01-04`
- **Jira**: `T-01`
- **Requirement**: `T01-05` (Naming convention: snake_case, id, created_at, updated_at)
- **Preconditions**: Các tệp SQL trong `backend/migrations/`
- **Steps**:
  1. Đọc và rà soát `001_baseline.sql`, `002_stations_and_charge_points.sql`, `003_stations_owner.sql`.
- **Expected**: 100% bảng đặt tên snake_case, primary key `id`, có `created_at` và `updated_at`.
- **Actual**: Tất cả bảng (`users`, `roles`, `user_roles`, `stations`, `charge_points`, `connectors`, `audit_logs`, `login_throttle`, `schema_migrations`) đều tuân thủ đúng quy ước.
- **Status**: **PASS**
- **Evidence**: [backend/migrations/001_baseline.sql](file:///c:/Users/Admin/Downloads/Charging-Station-Management-System-CSMS-/backend/migrations/001_baseline.sql).

### TC-T01-05
- **Test ID**: `TC-T01-05`
- **Jira**: `T-01`
- **Requirement**: `T01-NFR-01` (Database connection string từ environment)
- **Preconditions**: `docker-compose.yml` và `backend/src/db/pool.js`
- **Steps**:
  1. Kiểm tra cấu hình `Pool` trong `src/db/pool.js`.
  2. Kiểm tra ánh xạ biến môi trường trong `docker-compose.yml`.
- **Expected**: Đọc `DATABASE_URL` từ environment.
- **Actual**: `docker-compose.yml` nạp `DATABASE_URL` từ biến môi trường `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`; `pool.js` sử dụng `env.DATABASE_URL`.
- **Status**: **PASS**
- **Evidence**: [docker-compose.yml dòng 40](file:///c:/Users/Admin/Downloads/Charging-Station-Management-System-CSMS-/docker-compose.yml#L40); [backend/src/db/pool.js dòng 5](file:///c:/Users/Admin/Downloads/Charging-Station-Management-System-CSMS-/backend/src/db/pool.js#L5).

---

## 4. Current Status Summary

| Test ID | Jira | Requirement | Status | Notes |
|:---|:---|:---|:---:|:---|
| `TC-S01-01` | S-01 | S01-AC-01 | **PASS** | App + DB chạy, trang chủ HTTP 200 |
| `TC-S01-02` | S-01 | S01-NFR-01 | **PASS** | Secret nạp từ env, no-backdoor PASS 3/3 |
| `TC-S01-03` | S-01 | S01-NFR-02 | **PASS** | Log không lộ URI/credentials, chỉ in tên biến |
| `TC-T01-01` | T-01 | T01-01 / T01-02 | **PASS** | Docker Compose Up, kết nối PostgreSQL thành công |
| `TC-T01-02` | T-01 | T01-03 | **PASS** | Migration forward sạch sẽ |
| `TC-T01-03` | T-01 | T01-04 | **PASS** | Migration rollback thành công trên db_test |
| `TC-T01-04` | T-01 | T01-05 | **PASS** | 100% schema chuẩn snake_case, timestamps |
| `TC-T01-05` | T-01 | T01-NFR-01 | **PASS** | DATABASE_URL nạp từ biến môi trường |

### Overall Story & Task Status
- **S-01**: **PASS**
- **T-01**: **PASS**

### Current Blockers
- Không còn blocker nào ảnh hưởng đến S-01 và T-01 trên môi trường Docker.
